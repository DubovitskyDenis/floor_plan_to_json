# Floor Plan to JSON Parser

Прототип для извлечения архитектурной геометрии (стены) из изображений планов этажей и конвертации в JSON формат, пригодный для 2D/3D визуализации.

## 🎯 Цель проекта

Показать подход к автоматическому извлечению геометрии стен из растровых изображений планов и преобразованию в структурированный JSON формат.

## 🔧 Технологический стек

### Выбранный подход: **Гибридный (OpenCV + классическая CV)**

**Почему этот стек?**

1. **OpenCV** - основной инструмент:
   - Отлично подходит для детекции линий в архитектурных чертежах
   - Не требует обучения моделей (быстрый старт)
   - Эффективен для обработки технических чертежей с четкими границами
   - Легко интегрируется с другими инструментами

2. **Hough Transform (Probabilistic)**:
   - Классический метод для детекции прямых линий
   - Хорошо работает на бинаризованных изображениях
   - Быстрый и надежный для архитектурных планов

3. **LSD (Line Segment Detector)** - альтернативный метод:
   - Более точный для тонких линий
   - Лучше справляется с размытыми границами
   - Медленнее, но качественнее

4. **Почему не нейросети (YOLO/UNet/SAM)?**
   - Для прототипа классический CV быстрее в разработке
   - Не требует размеченных данных для обучения
   - Архитектурные планы имеют четкую структуру (линии), где классические методы работают хорошо
   - Нейросети были бы полезны для более сложных случаев (перспектива, шум, рукописные планы)

## 📋 Этапы пайплайна

### 1. Предобработка изображения
- Конвертация в grayscale
- Бинаризация (Otsu thresholding)
- Автоматическая инверсия (если фон светлый)
- Морфологические операции (закрытие/открытие) для очистки шума

### 2. Детекция линий
- **Вариант A**: Probabilistic Hough Transform (`HoughLinesP`)
  - Быстрый и эффективный для четких линий
- **Вариант B**: Line Segment Detector (LSD)
  - Более точный, но медленнее

### 3. Постобработка
- Фильтрация по минимальной длине
- Объединение близких и коллинеарных линий
- Удаление дубликатов

### 4. Экспорт в JSON
- Структурированный формат с метаданными
- Координаты в пикселях (можно масштабировать позже)

## 📦 Установка

```bash
pip install -r requirements.txt
```

## 🚀 Использование

### Базовое использование

```bash
python floor_plan_parser.py --input plan1.png plan2.jpg plan3.png
```

### С дополнительными опциями

```bash
# Использовать LSD детектор (медленнее, но точнее)
python floor_plan_parser.py --input plan1.png --lsd

# Изменить минимальную длину линии
python floor_plan_parser.py --input plan1.png --min-length 30

# Указать выходной файл
python floor_plan_parser.py --input plan1.png --output results.json
```

### Программное использование

```python
from floor_plan_parser import FloorPlanParser

parser = FloorPlanParser(min_line_length=50)
result = parser.process_image("plan.png")
print(result)
```

## 📄 Формат выходного JSON

```json
[
  {
    "meta": {
      "source": "test_01.png"
    },
    "walls": [
      {
        "id": "w1",
        "points": [[100, 200], [300, 200]]
      },
      {
        "id": "w2",
        "points": [[300, 200], [300, 400], [100, 400]]
      }
    ]
  }
]
```

## ✨ Бонусные функции

### Детекция дверей и окон

Реализована базовая детекция дверей и окон через:
- Поиск разрывов в стенах
- Детекция прямоугольных объектов определенного размера
- Анализ контуров и классификация по форме (вертикальные = двери, горизонтальные = окна)

Использование:
```bash
python floor_plan_parser.py --input plan.png --detect-openings
```

Формат выходных данных с отверстиями:
```json
{
  "meta": {
    "source": "test_01.png"
  },
  "walls": [...],
  "openings": [
    {
      "id": "o1",
      "type": "door",
      "points": [[x1, y1], [x2, y2], [x3, y3], [x4, y4]],
      "bbox": {"x": 100, "y": 200, "width": 50, "height": 150},
      "area": 7500.0
    }
  ]
}
```

## ⚠️ Слабые места текущей реализации

1. **Координаты в пикселях**: Нет масштабирования в реальные единицы измерения
2. **Перспектива**: Не обрабатывает фотографии с перспективными искажениями
3. **Сложная геометрия**: Может пропускать криволинейные стены
4. **Пересечения**: Не всегда корректно обрабатывает пересечения стен
5. **Шум**: На "грязных" фотографиях может находить ложные линии
6. **Толщина стен**: Не различает толщину стен (все линии одинаковые)
7. **Окна/двери**: Базовая детекция, не всегда точная

## 🔮 Что бы улучшили в следующей итерации

### Краткосрочные улучшения (1-2 недели):
1. **Калибровка масштаба**: Добавить OCR для размеров и автоматическое масштабирование
2. **Улучшение детекции**: Комбинировать Hough и LSD результаты
3. **Фильтрация шума**: Добавить более продвинутые методы очистки
4. **Обработка перспективы**: Добавить коррекцию перспективы для фотографий

### Среднесрочные улучшения (1-2 месяца):
1. **Нейросети для сложных случаев**: 
   - Использовать UNet для сегментации стен
   - YOLO для детекции дверей/окон
   - SAM (Segment Anything) для интерактивной сегментации
2. **3D реконструкция**: Восстановление высоты стен из планов
3. **Распознавание помещений**: Группировка стен в комнаты
4. **Валидация**: Проверка геометрической корректности (замкнутость, пересечения)

### Долгосрочные улучшения (3+ месяца):
1. **End-to-end модель**: Обучение специализированной модели на датасете планов
2. **Мультимодальность**: Обработка разных типов планов (электрика, сантехника)
3. **Интерактивность**: Веб-интерфейс для коррекции результатов
4. **Экспорт в CAD**: Прямой экспорт в DXF/DWG форматы

## 📊 Визуализация результатов

Для визуализации найденных стен и отверстий на исходных изображениях используйте скрипт `visualize_results.py`:

```bash
# Базовое использование
python visualize_results.py --json output.json --images-dir . --output-dir visualized

# Без отображения отверстий
python visualize_results.py --json output.json --no-openings

# Без меток ID
python visualize_results.py --json output.json --no-labels

# Настройка цвета и толщины стен
python visualize_results.py --json output.json --wall-color 255 0 0 --wall-thickness 3
```

Скрипт создаст визуализации с:
- Зелеными линиями для стен
- Цветными контурами для отверстий (синий - двери, оранжевый - окна)
- Легендой в углу изображения
- Метками ID элементов (опционально)

## 📝 Примеры использования

См. папку `examples/` для примеров входных изображений и результатов.

## 🤝 Лицензия

MIT License

CubiCasa5K выбран, так как он содержит реальные планы квартир с аннотациями стен, дверей и помещений, что напрямую соответствует задаче извлечения архитектурной геометрии. Датасет широко используется в академических и индустриальных работах, что снижает риски переобучения на синтетике.